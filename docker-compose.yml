version: "3.8"

services:
  ilantakip-bot:
    build: .
    container_name: ilantakip-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
